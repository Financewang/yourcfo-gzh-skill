# GEO 怎么用

这是 [使用说明.md](使用说明.md) 的补充。排版那套流程不变，GEO 是在它前后各加一步。

## 一句话

**GEO 是让 AI 在回答问题时引用你、并指到你的出处。** 和 SEO 的区别：SEO 追求被点击，GEO 追求被引用。

## 写稿前：绑问句

去私有仓库 `YourCFO/geo/prompts.yaml` 里找 1–3 条最贴这篇的问句，把它记在稿子里：

```markdown
<!-- geo:prompts Q004,Q008 -->
```

没有合适的问句怎么办？先往 `prompts.yaml` 里加一条再写。**问句不是装饰，它是这篇稿子的靶心。**

## 写稿时：写一句答案句

答案句 = 这一篇里最该被 AI 整句摘走的那句话。放在前 300 字内，别超过 60 字：

```markdown
<!-- geo:answer 三个环节各 20%：装入（财产转让所得）、存续（不分配也按年计税）、终止清算 -->
```

答案句要能独立成立——被摘走之后，不需要上下文也读得通。

末尾加一节常见问题，至少两组：

```markdown
## 常见问题

**问：离岸信托该在哪儿交税？**

答：15 号公告确立主管税务机关三级顺位，第一顺位是境内主要经营企业注册地。

**问：补申报现在能线上办吗？**

答：不能，目前只能线下办税服务厅。
```

最后记一下这一篇的更新日期（以后每季度回来看要不要改）：

```markdown
<!-- geo:updated 2026-09-22 -->
```

## 排版前：GEO 体检

```cmd
cd /d C:\Users\Jack\yourcfo-gzh

python scripts\geo_audit.py drafts\稿件.md --prompts ..\YourCFO\geo\prompts.yaml
```

六项检查，ERROR 清零再往下走。

| 代码 | 含义 | 处理 |
|---|---|---|
| GA-01 | 没有答案句 | 补 `<!-- geo:answer … -->` |
| GA-02 | 答案句超过 60 字 | 拆成两句，一句一个判断 |
| GA-03（WARN） | 答案句没有写进正文 | 把同一句判断写进首段 |
| GA-03（ERROR） | 答案句在正文里，但位置超过前 300 字 | 挪到开头 |
| GA-04 | 没绑问句 / 绑了超过 3 条 | 1–3 条之间 |
| GA-05 | 绑的问句 ID 不在库里 | 检查拼写，或先把问句入库 |
| GD-01（WARN） | 数据密度低于每 150 字一个可验证声明 | 补数字、日期、文号 |
| GR-01（WARN） | 外部权威引用少于 3 处 | 补官方原文、答记者问、官方案例 |
| GF-01 | 没有 FAQ 章节，或不足两组问答 | 末尾补「## 常见问题」 |
| GU-01（WARN） | 更新日期缺失或超过 90 天 | 补 `<!-- geo:updated … -->` |

## 发布后：分发

公众号是主阵地（腾讯元宝与微信搜一搜把公众号当第一来源），但**豆包、文心、DeepSeek、Kimi 抓不到公众号**。所以每篇发完之后：

- **头条号**——把核心判断写成一篇图文（对应豆包）
- **百家号**——同步发（对应文心）
- 星球深度文顺手转**搜狐号**（对应 Kimi，它偏好 3000 字以上长文）

平台机制与优先级写在 `config/geo.yaml`，要调就改那里。

## 每月一次：五平台体检

1. 从 `prompts.yaml` 挑 5–10 条问句，**用口语原话**（不要用关键词）分别在元宝、豆包、文心、DeepSeek、Kimi 各问一遍
2. 记录三件事：有没有出现你的内容、引用了哪一句、出处落在哪个平台
3. 写进 `YourCFO/geo/citations-log/YYYY-MM.md`，覆盖情况更新到 `geo/coverage.md`
4. 没被引用的，只查两处：站外有没有可被抓取的版本；答案句够不够自包含

## 攒经验的地方

每发现一条新的高频问句，加进 `geo/prompts.yaml`：

```yaml
  - id: Q015
    prompt: 问句原话（用户会怎么问）
    line: 主线
    intent: 定义|判定|场景|对比
    answer: 一句 ≤60 字的判断
    support: [支撑的文章日期]
    published: []
    status: covered
    citations: []
```

犯过的错只犯一次——问句库用半年，别人就很难追上。
