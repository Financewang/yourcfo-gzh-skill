---
name: fiscal-gzh
description: 跨境财税合规类公众号文章排版与内容审计工具。把 Markdown 文稿转换为可直接粘贴进微信公众号编辑器的 HTML，并对内容结构做确定性审计。区别于通用排版 skill：本 skill 把法条文号、截止日倒计时、金额对照、对号入座分类、来源等级、历史回指当作一等公民处理，并内置反 AI 体词表与素材核实校验。触发场景：(1) 用户提到"排版""公众号 HTML""转成公众号格式"且文章属于财税/法律/合规类；(2) 用户要求对已写好的合规类文稿做发布前审计；(3) 用户提到"倒计时校验""文号校验""AI 体检查"。不用于生活类、营销类、纯技术类文章排版（用通用 gzh skill）。
---

# 跨境财税合规公众号排版 Skill

把一篇合规类文稿转成公众号可粘贴的 HTML，同时做**内容结构审计**。

和通用排版 skill 的根本区别：通用 skill 解决的是"好不好看"，本 skill 解决的是"能不能发"。合规类内容的返工点不在配色，在于——倒计时数字和发布日对不上、引了法条没写文号、具体金额没有来源、AI 体句式混进正文、CTA 出现两次。这些错误排版再漂亮也得撤回重发。

核心资产是两份：`references/compliance-components.md`（合规专用组件库，本 skill 的独有部分）和两套主题库。**审计脚本 `scripts/content_audit.py` 是强制环节，不是可选项。**

## 工作流

### 1. 读稿，判定文章层级

先判定这篇属于哪一层，层级决定骨架和 CTA 策略：

| 层级 | 特征 | 骨架 | 结尾 |
|------|------|------|------|
| 入口层 | 有硬时效、事件驱动、覆盖面宽 | 倒计时条置顶 | 指向次日文章，**不放星球 CTA** |
| 转化层 | 边界澄清、对号入座、纠偏 | 反直觉结论前置 | 星球 CTA |
| 沉淀层 | 案例复盘、过程还原 | 场景开场 | 星球 CTA |

判定依据：标题含截止日/具体日期 → 入口层；标题是判断句或疑问句、正文有三类以上分类 → 转化层；正文以具体人物或案子开场 → 沉淀层。

拿不准就问用户，不要猜。

### 2. 选主题

只有两套，按紧迫程度选，不按题材选：

- **政策深蓝**（`theme-regulatory-navy.md`）——默认。政策解读、边界澄清、案例复盘都用这套。
- **倒计时琥珀**（`theme-deadline-amber.md`）——仅用于有硬截止日且剩余天数 ≤ 30 天的文章。整篇的视觉重心是那个日期。

不做题材匹配，不做多主题选择。垂直账号要的是辨识度，不是多样性。同一个账号每天换风格，读者认不出来。

### 3. 读组件库

Read `references/compliance-components.md` + 所选主题库。

**先查合规组件库**：法条、文号、截止日、金额对照、对号入座、回指、来源标注、更正声明——这七类必须用合规库的组件，主题库只提供颜色。主题库负责的是标题、正文、引言、CTA 这些通用件。

### 4. 结构映射

| Markdown 写法 | 映射组件 | 来源 |
|---|---|---|
| `> 法条：xxx` 或正文出现"第X条规定" | C1 条文卡 | 合规库 |
| `!!倒计时 2026-10-22!!` | C2 倒计时条 | 合规库 |
| `### 三种情况，对号入座` 后接的分段 | C3 对号入座块 | 合规库 |
| 两个以上金额并列对比 | C4 金额对照表 | 合规库 |
| `[L1]` `[L2]` `[L3]` 标记 | C5 来源等级标 | 合规库 |
| "X月X日那篇" | C6 回指卡 | 合规库 |
| `!!更正!!` | C7 更正声明块 | 合规库 |
| `## 标题` | 章节标题 | 主题库 |
| `**加粗**` | 主色加粗 | 主题库 |
| 结尾固定段 | 星球 CTA 卡 | 主题库 |

### 5. 内容审计（强制，先于 HTML 校验）

```bash
<SKILL_ROOT>/scripts/content_audit.py <稿件.md 或 生成的.html> --pubdate 2026-09-18
```

七项检查，任何一项 ERROR 都必须改完再排版：

1. **倒计时一致性**——正文出现"还剩 N 天"时，用 `--pubdate` 算出真实天数比对。发布日一改，这个数就错，这是本账号最高频的返工点。
2. **文号完整性**——出现"X 号公告""X 号文""X 号令"但全文没有对应全称或文号的，报 ERROR。首次出现必须全称加文号。
3. **金额溯源**——出现具体金额（万元/亿元/美元）但所在段落及前后各一段内无来源标识（机关名/文件名/媒体名/`[Lx]` 标记）的，报 WARNING，逐条人工确认。
4. **AI 体词表**——命中禁用句式报 ERROR。词表见 `references/anti-ai-lexicon.md`，包含拟人化法规、"X 的是"归纳句、归拢式收尾、空转过渡词等。
5. **CTA 唯一性**——星球 CTA 出现 0 次或 2 次以上报 ERROR；入口层文章出现星球 CTA 报 WARNING。
6. **半角标点**——中文语境内的半角标点和直引号。
7. **法规名称核对**——内置易错名称表（如 837 号令的正式名称是《国务院关于对外投资的规定》），写错报 ERROR。

### 6. 平台合规校验

```bash
<SKILL_ROOT>/scripts/validate_gzh_html.py <生成的.html>
```

ERROR 清零。检查禁用标签属性和 `<span leaf>` 包裹。

### 7. 输出

产物是纯 `<section>` 片段，不带文档外壳。文件名 `{原名}_排版_{主题}.html`，同时产出 `_预览.html`（带复制按钮）。

交付时附三样：审计结论、校验结论、本篇判定的层级和理由。

## 本 skill 的七个独有处理

**一、倒计时是活的**
所有倒计时组件写入时同时写一个 HTML 注释 `<!-- deadline:2026-10-22 -->`，审计脚本据此重算。改发布日时重跑脚本即可发现不一致。

**二、法条引用与普通引用分开**
法条走 C1 条文卡（左侧色条 + 文号标注在卡片顶部），普通引述走主题库的引用块。读者要能一眼分出哪句是原文、哪句是转述。这是合规类内容的基本诚信。

**三、首次出现必须全称加文号**
"21 号公告"第一次出现时写《财政部 税务总局关于离岸信托个人所得税有关事项的公告》（财政部 税务总局公告 2026 年第 21 号），之后才可简称。今年号文密集，孤立的编号会被读者张冠李戴。

**四、对号入座是结构件不是列表**
三类情况用 C3 组件（编号圆标 + 加粗标题 + 说明段），不要降级成普通有序列表。这个结构是转化层文章收藏率的主要来源，读者要靠它自我归类。

**五、来源等级可选可见**
`[L1]` 到 `[L3]` 标记在正文里默认不渲染出来，只进审计。用户显式要求时才渲染成 C5 角标。写作阶段自己标，发布时决定露不露。

**六、回指是硬要求**
转化层文章必须至少有一处 C6 回指卡。没有的报 WARNING。账号的延续性靠这个撑，每篇都从零讲起的号留不住人。

**七、更正走独立组件**
写错过的地方用 C7 更正声明块，样式上明确区别于正文，不藏在段落里。这是账号信誉的组成部分。

## 视觉层级（两套主题通用）

| 层级 | 用途 | 频率 |
|---|---|---|
| 一级 | 截止日、核心金额、反直觉结论 | 全文 ≤ 3 处 |
| 二级 | 法条关键词、分类标题 | 每节 1~2 处 |
| 三级 | 正文关键短语下划线 | 每段 1~2 处 |

合规类文章的强调要比通用文章更克制。满篇高亮会让读者怀疑内容在虚张声势，而这个领域最贵的东西是可信度。

## 平台红线

禁止：`<style>`/`<script>`/`<div>`、`class`/`id`、`position:fixed|absolute|sticky`、`float`、`@media`/`@keyframes`、`display:grid`、CSS 变量、外部字体。

必须：样式全内联；所有文字节点 `<span leaf="">` 包裹。

可用：`display:flex`（有限）、`linear-gradient`、`border-radius`、`box-shadow`、`<section>/<p>/<span>/<strong>/<img>`。

## Gotchas

- **倒计时不重算**——改日期发布是最常见的翻车方式，必跑审计脚本。
- **文号简称先行**——正文第一次就写简称，读者对不上是哪份文件。
- **金额无出处**——合规类内容里，一个没有来源的具体数字比没有数字更伤。
- **对号入座降级成列表**——失去自我归类的功能，转化层就废了一半。
- **CTA 重复**——原文末尾已有 CTA 时要并入，不要再生成一个。
- **入口层放星球 CTA**——入口层读者还不是目标客户，硬转化拉低完读率。
- **法条卡滥用**——只有真的引原文才用，转述一律走普通引用。
- **AI 体词混入**——排版阶段不改文字，但审计命中要退回作者改，不要自行改写原意。
- **主题混用**——两套主题不共存于一篇。
- **图片拉伸**——`max-width:100%;height:auto`，不用 `width:100%`。

## 扩展

新增合规组件写入 `references/compliance-components.md` 并在 `content_audit.py` 的 `COMPONENT_MARKERS` 登记；新增易错法规名写入脚本的 `REGULATION_NAMES` 表；新增 AI 体词条写入 `references/anti-ai-lexicon.md`。
