# 怎么用

## 一次性准备

装 Python（python.org 下载，安装时勾选 Add Python to PATH）。装完在 cmd 里敲 `python --version` 能出版本号就行。

## 日常流程

把写好的稿子存成 `.md` 放进 `drafts` 文件夹（没有就自己建一个），然后在 cmd 里：

```cmd
cd /d C:\Users\Jack\yourcfo-gzh

REM 第一步：审计内容
python scripts\content_audit.py drafts\稿件.md --pubdate 2026-09-18 --layer 转化

REM 第二步：排版（审计通过后）
python scripts\render.py drafts\稿件.md --theme navy --layer 转化 --pubdate 2026-09-18

REM 第三步：校验平台合规
python scripts\validate_gzh_html.py "drafts\稿件_排版_政策深蓝.html"

REM 第四步：生成预览页
python scripts\wrap_preview.py "drafts\稿件_排版_政策深蓝.html"
```

最后双击打开 `_预览.html`，点右上角「复制到公众号」，到公众号编辑器 Ctrl+V。

**找不到预览页？** 它和排版页在同一个文件夹（`drafts`），文件名是 `xxx_排版_政策深蓝_预览.html`，比排版页多了「_预览」两个字。脚本运行完会打印完整路径，照着找就行。

**复制按钮点了没反应？** 直接在预览页正文区手动全选（Ctrl+A）再复制（Ctrl+C）也一样，效果完全相同。

## 参数怎么填

**--theme**：`navy` 政策深蓝（默认）；`amber` 倒计时琥珀，只在有硬截止日且剩余 ≤30 天时用。

**--layer**：`入口`（有时效、拉新，结尾指向次日，不放星球 CTA）；`转化`（边界澄清、对号入座，放星球 CTA）；`沉淀`（案例复盘，放星球 CTA）。

**--pubdate**：实际发布日，格式 2026-09-18。倒计时靠它重算。

## 稿子里能用的标记

普通 Markdown 之外，本 skill 认识这些：

```
!!倒计时 2026-10-22!!

!!法条 财政部 税务总局公告 2026 年第 21 号 · 第十七条 | 条文原文!!

!!对号入座!!
1. 第一类的标题 | 第一类的说明
2. 第二类的标题 | 第二类的说明

!!对照!!
未办登记，资金未流动 | 2.5 万元
实控人变更未登记 | 302 万元

!!回指!! 8 月 24 日那篇写过补登记的实际路径。

!!更正!! 9 月 10 日那篇里的某个说法不完整，正确的是……

!!预告!! 明天讲另一件事。

## 标题      → 带编号的大章节标题
### 标题     → 琥珀色左竖条小标题
#### 标题    → 更小一级标题
- 列表项     → 圆点列表
1. 列表项    → 数字列表（注意：行内不能带 | ，否则会被当成对号入座）
---          → 分割线

**加粗**    → 琥珀色强调，全文别超过 3 处
==关键词==  → 下划线，每段 1~2 处
> 开头第一个引用 → 引言块；之后的引用 → 普通引用块
```

## 审计报了 ERROR 怎么办

| 代码 | 含义 | 处理 |
|---|---|---|
| CD-01 | 倒计时天数和发布日对不上 | 改正文里的天数，或者改 `--pubdate` |
| CT-01 | 简称附近没有全称 | 首次出现处补《…》全称加文号 |
| RG-01 | 法规名称写错 | 按提示改 |
| AI-01 | 命中 AI 体句式 | 退回改写，不要硬绕 |
| CTA-01/02 | 星球 CTA 缺失或重复 | 保证有且仅有一处 |
| AM-01（WARN） | 金额附近没有来源 | 人工确认出处，确实有就忽略 |
| BR-01（WARN） | 转化层没有回指 | 加一处 `!!回指!!` |

## 攒经验的地方

每次发现新的易错法规名，在 `scripts/content_audit.py` 里找到 `REGULATION_NAMES`，加一行：

```python
REGULATION_NAMES = {
    "对外投资管理条例": "《国务院关于对外投资的规定》（国务院令第 837 号）",
    "新的错误写法": "正确名称",
}
```

改完提交：

```cmd
git add -A
git commit -m "补充易错法规名"
git push
```

犯过的错只犯一次，这张表用半年就很难被复制了。
